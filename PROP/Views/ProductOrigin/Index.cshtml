@model IEnumerable<PROP.Models.ProductOrigin>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Origin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Origin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OriginId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OriginId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OriginId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
